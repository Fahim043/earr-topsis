services:
  - type: web
    name: earr-topsis-api
    env: python
    rootDir: web_api
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
